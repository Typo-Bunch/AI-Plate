{
  "name": "ai_plate",
  "productName": "AI Plate",
  "version": "1.0.0-rc.1",
  "description": "AI Plate — Open Power: Universal AI Agent Harness with Multi-Provider Support and Sandboxed Execution",
  "author": "AI Plate Team",
  "license": "MIT",
  "type": "module",
  "main": "dist/electron/main.js",
  "scripts": {
    "build": "tsc && node -e \"const fs = require('fs'); fs.mkdirSync('dist/electron', {recursive:true}); fs.copyFileSync('electron/preload.cjs', 'dist/electron/preload.cjs'); console.log('\\x1b[32m✔ Build completed successfully! TypeScript compiled to dist/\\x1b[0m');\"",
    "start": "electron .",
    "dev": "npm run build && electron .",
    "dist": "npm run build && electron-builder --config electron-builder.json",
    "dist:setup": "npm run build && electron-builder --win nsis --config electron-builder.json",
    "dist:dir": "npm run build && electron-builder --win --dir --config electron-builder.json",
    "setup:python": "node scripts/setup-bundled-python.js",
    "test": "node --import tsx --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@google/genai": "^1.0.0",
    "adm-zip": "^0.6.0",
    "better-sqlite3": "^13.0.3",
    "dotenv": "^16.4.7",
    "openai": "^7.5.0",
    "pdf-parse": "^2.4.5",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.8",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22.10.0",
    "@types/pdf-parse": "^1.1.5",
    "electron": "^44.1.1",
    "electron-builder": "^26.15.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
